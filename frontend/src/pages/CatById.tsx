import PageWrapper from '@/components/common/PageWrapper';
import { Typography } from '@mui/material';

type Props = {};

function CatById({}: Props) {
  return (
    <PageWrapper>
      <Typography>CatById</Typography>;
    </PageWrapper>
  );
}

export default CatById;
